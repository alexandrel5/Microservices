Commands helm

helm ls

#To build dependency
helm dependency build

#helm install nome folder
helm install grafana grafana

#
helm uninstall eazybank

kubectl get services

helm history eazybank

helm rollback eazybank 1
